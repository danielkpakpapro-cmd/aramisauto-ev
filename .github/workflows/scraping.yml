name: Scraping Aramisauto

on:
  schedule:
    - cron: '0 6 * * *'
  workflow_dispatch:

jobs:
  scrape:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Recuperer le depot
        uses: actions/checkout@v4

      - name: Installer Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Installer les dependances
        run: |
          pip install -r requirements-scraping.txt
          playwright install --with-deps chromium

      - name: Lancer le scraping
        run: python scraper.py

      - name: Pousser les donnees mises a jour
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add data/historique.csv
          git diff --cached --quiet && echo "Aucun changement." || (git commit -m "Mise a jour automatique" && git pull --rebase origin main && git push)
